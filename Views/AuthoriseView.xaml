<Page x:Class="WpfMvvmApp.Views.AuthoriseView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfMvvmApp.Views"
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="600"
      Title="Authorise">

    <Grid Background="#F0F0F0">
        <Border Background="White" CornerRadius="10" Padding="30" VerticalAlignment="Center" HorizontalAlignment="Center" Width="400">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.2"/>
            </Border.Effect>
            
            <StackPanel>
                <TextBlock Text="Authorise" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30" Foreground="#333"/>
                
                <TextBlock Text="Username" FontSize="16" Foreground="#666" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Username}" FontSize="18" Padding="10" Height="50" Margin="0,0,0,20" VerticalContentAlignment="Center"/>
                
                <TextBlock Text="Password" FontSize="16" Foreground="#666" Margin="0,0,0,5"/>
                <PasswordBox x:Name="UserPasswordBox" FontSize="18" Padding="10" Height="50" Margin="0,0,0,40" VerticalContentAlignment="Center"/>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <!-- Spacing property might not be available in older WPF, using Margin instead via style or manual -->
                    <Button Content="OK" 
                            IsDefault="True"
                            Command="{Binding OkCommand}" 
                            CommandParameter="{Binding ElementName=UserPasswordBox}"
                            Width="140" Height="60" FontSize="20" FontWeight="SemiBold"
                            Background="#007ACC" Foreground="White" BorderThickness="0"
                            Margin="0,0,10,0">
                         <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                    
                    <Button Content="Cancel" 
                            Command="{Binding CancelCommand}" 
                            Width="140" Height="60" FontSize="20" FontWeight="SemiBold"
                            Background="#E0E0E0" Foreground="#333" BorderThickness="0"
                            Margin="10,0,0,0">
                         <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>
