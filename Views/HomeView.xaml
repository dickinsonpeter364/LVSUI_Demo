<Page x:Class="WpfMvvmApp.Views.HomeView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfMvvmApp.Views"
      xmlns:vm="clr-namespace:WpfMvvmApp.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HomeView">
    <d:Page.DataContext>
        <vm:HomeViewModel/>
    </d:Page.DataContext>

    <Grid Background="White">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="{Binding WelcomeMessage}" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
            <Button Content="Go to Settings" Command="{Binding NavigateToSettingsCommand}" Width="150" Height="40" Margin="0,0,0,10"/>
            <Button Content="Go to Authorise" Command="{Binding NavigateToAuthoriseCommand}" Width="150" Height="40" Margin="0,0,0,10"/>
            <Button Content="Device Control" Command="{Binding NavigateToDeviceControlCommand}" Width="150" Height="40" Margin="0,0,0,10"/>
            <Button Content="Start Inspection" Command="{Binding NavigateToInspectionCommand}" Width="150" Height="40"/>
        </StackPanel>
    </Grid>
</Page>
