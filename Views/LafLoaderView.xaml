<Page x:Class="WpfMvvmApp.Views.LafLoaderView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfMvvmApp.Views"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="LAF Loader">

    <Grid Background="#F0F0F0">
        <Border Background="White" CornerRadius="10" Padding="30" VerticalAlignment="Center" HorizontalAlignment="Center" Width="700">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.2"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="Load LAF Files" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30" Foreground="#333"/>

                <!-- LAF 1 Selection -->
                <TextBlock Text="LAF 1" FontSize="16" FontWeight="SemiBold" Foreground="#444" Margin="0,0,0,5"/>
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Text="{Binding Laf1Path}" IsReadOnly="True" FontSize="16" Padding="5" Height="40" VerticalContentAlignment="Center" Background="#FAFAFA"/>
                    <Button Grid.Column="1" Content="..." Command="{Binding SelectLaf1Command}" Width="50" Height="40" Margin="10,0,0,0" Background="#EEE" Foreground="#333"/>
                </Grid>

                <!-- LAF 2 Selection -->
                <TextBlock Text="LAF 2" FontSize="16" FontWeight="SemiBold" Foreground="#444" Margin="0,0,0,5"/>
                <Grid Margin="0,0,0,40">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Text="{Binding Laf2Path}" IsReadOnly="True" FontSize="16" Padding="5" Height="40" VerticalContentAlignment="Center" Background="#FAFAFA"/>
                    <Button Grid.Column="1" Content="..." Command="{Binding SelectLaf2Command}" Width="50" Height="40" Margin="10,0,0,0" Background="#EEE" Foreground="#333"/>
                </Grid>

                <!-- Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="OK" 
                            IsDefault="True"
                            Command="{Binding OkCommand}" 
                            Width="150" Height="50" FontSize="18" FontWeight="SemiBold"
                            Background="#007ACC" Foreground="White" BorderThickness="0"
                            Margin="0,0,20,0">
                         <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                    </Button>

                    <Button Content="Cancel" 
                            Command="{Binding CancelCommand}" 
                            Width="150" Height="50" FontSize="18" FontWeight="SemiBold"
                            Background="#E0E0E0" Foreground="#333" BorderThickness="0">
                         <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>
