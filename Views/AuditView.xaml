<Page x:Class="WpfMvvmApp.Views.AuditView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:local="clr-namespace:WpfMvvmApp.Views"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="800"
      Title="Audit View">

    <Grid Background="#F0F0F0">
        <Grid.RowDefinitions>
             <RowDefinition Height="Auto"/>
             <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Background="White" Padding="20" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid>
                <Button Command="{Binding GoBackCommand}" HorizontalAlignment="Left" Width="50" Height="50" Background="Transparent" BorderThickness="0" Content="&lt;" FontSize="24" Foreground="#333"/>
                <TextBlock Text="Audit View" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#333"/>
            </Grid>
        </Border>

        <!-- Content -->
        <Border Grid.Row="1" Margin="20" Background="White" BorderBrush="#CCC" BorderThickness="1">
            <TextBlock Text="Audit Trail Placeholder" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" Foreground="#888"/>
        </Border>
    </Grid>
</Page>
